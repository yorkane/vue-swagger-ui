import * as types from './mutation-types'

export const addApiDocs = ({ commit }, apiDocs) => commit(types.ADD_API_DOCS, apiDocs)

export const setBaseUrl = ({ commit }, baseUrl) => commit(types.ADD_API_DOCS, baseUrl)
